{
  "name": "beat-mania-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-snapshot.js && node -e \"require('fs').accessSync('dist/index.html'); require('fs').accessSync('dist/engine.snapshot.json')\"",
    "preview": "vite preview",
    "engine:verify": "node scripts/engine-verify.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
